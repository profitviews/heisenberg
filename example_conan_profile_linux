# To use this file in a build, place it in ~/.conan/profiles/ (and probably rename it)
# Then, invoke the `conan install` step of README.md as:
# `conan install ../ --build missing --profile example_conan_profile_linux`
#
# Modify it according to https://docs.conan.io/en/latest/reference/profiles.html

[settings]
os=Linux
os_build=Linux
arch=x86_64
arch_build=x86_64
compiler=gcc
compiler.version=11
compiler.libcxx=libstdc++11
build_type=Debug
